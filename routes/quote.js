const express = require('express');
const router = express.Router();

// @desc    Get a random quote
// @route   GET /api/quote
router.get('/', (req, res) => {

});


// @desc    Get a quote for the given ID
// @route   GET /api/quote/:id
router.get('/:id', (req, res) => {

});

// @desc    Create a quote
// @route   POST /api/quote
router.post('/', (req, res) => {

});

module.exports = router;